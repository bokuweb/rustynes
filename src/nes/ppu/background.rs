use std::cell::Cell;

use super::types::Addr;

#[derive(Debug)]
pub struct Background {
}

impl Background {
    pub fn new() ->Self {
        Background {
        }
    }

    pub fn build(&self) -> Background {

    }
}
